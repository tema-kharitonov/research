<prompt>

## 🧑‍💻 Role
<role>
You are an extraction and normalization engine.
</role>

## 📥 Input
<input>
The project contains 20 attached files: raw_research1.pdf … raw_research20.pdf
</input>

## 📤 Output
<output>
One file: normalized_research.md
</output>

## 🎯 Goal
<goal>
Convert ALL explicitly described pre-registration user acquisition examples from ALL 20 files into normalized atomic entries. Do NOT miss a single example. Completeness has absolute priority over cleanliness.
</goal>

## 🔁 Mandatory Process (Strict)
<instructions>
  <step index="1">Process files STRICTLY SEQUENTIALLY: raw_research1.pdf → raw_research2.pdf → … → raw_research20.pdf.</step>
  <step index="2">
    In EACH file:
    - read the entire document top to bottom
    - extract EVERY fragment that describes:
      • Product
      • Link
      • Description of product
      • Mechanism type
      • Description of mechanism
      • Acqusition
      • jtbd
      • Source
  </step>
  <step index="3">
    EACH mechanism = ONE atomic entry.
    - If a paragraph contains multiple mechanisms — split them.
    - If mechanisms repeat across files — duplicates are allowed.
  </step>
  <step index="4">NEVER decide importance. If in doubt — extract.</step>
</instructions>

## ✅ Strict Rules
<rules>
  <rule>Write ONLY in Russian.</rule>
  <rule>Do NOT invent, assume, interpret, summarize, or generalize.</rule>
  <rule>Do NOT classify, group, or merge mechanisms.</rule>
  <rule>Do NOT improve wording.</rule>
  <rule>You MAY minimally cut or rearrange text ONLY to isolate one mechanism.</rule>
  <rule>If information is missing — leave the field empty.</rule>
  <rule>If the source text is vague or uncertain — keep it vague.</rule>
  <rule>Use ONLY information from the attached files.</rule>
</rules>

## 🧾 Normalized Output Format (Strict)
<format>
Produce ONE Markdown file: normalized_research.md.

Each entry MUST follow EXACTLY this structure:

- Product
  &lt;product name as written in source, or "unknown"&gt;

- Link
  &lt;official URL if explicitly present in source, otherwise empty&gt;

- Description of product
  &lt;максимально близко к источнику, например, после "Описание"&gt;

- Mechanism type
  &lt;бери строго из источника&gt;

- Description of mechanism
  &lt;verbatim or максимально близко к источнику description&gt;

- Acqusition
  &lt;get from Discovery&gt;

- jtbd
  &lt;get from User outcome&gt;

- Source
  &lt;links or references exactly as provided in the source files&gt;
</format>

## 🧩 Canonical Template Requirement
<template>
The project contains a file: normalized_research_etalon.md.
This file is the CANONICAL FORMAT TEMPLATE.

Before producing any output:
- Open normalized_research_etalon.md
- Identify the exact structure of ONE atomic entry (field names, order, punctuation, spacing, blank lines)

You MUST reproduce this structure EXACTLY for every extracted entry.

Any deviation from the template is forbidden:
- no renaming fields
- no reordering
- no extra fields
- no missing fields
- no formatting changes
</template>

## ✅ Final Check (Mandatory)
<final_check>
Before finishing:
- confirm that EACH of the 20 files produced extracted entries
- if any file yields zero entries, re-read it entirely
- prefer over-extraction to omission
</final_check>

## 🚫 Output Constraints
<output_constraints>
OUTPUT ONLY: normalized_research.md
No explanations.
No summaries.
No commentary.
</output_constraints>

</prompt>
